# Good compromise btw compile speed and executions speed
OPT = -O1

# NOTE: Do NOT add bounds check here; that should be used only in full_debug (slows execution down a lot!)
DEBUG = -g -traceback -ftrapuv -fp-model source -fp-model except -check pointers,uninit -DCHECK_NAN
