Different resolution
-------------------------

If the two task differ in resolution, with a constant ratio, and has aligned cells,
then the only difference is that the flux contributions need to be normalized by
the cell area, and needs to be summed over the same areas.


.. toctree::
   :maxdepth: 4
